<div class="row">
    <div class="col-md-12">
        <div class="form-group has-primary">
            <label for="{{ $name }}">
                <strong>{{ $label }}</strong>
            </label>
            <select name="{{ $name }}[]" id="{{ $name }}" class="form-control select2_search_tags" multiple></select>
        </div>
    </div>
</div>

